

<% if offers.exists? %>

<table class="table table-hover table-sm">

    <% offers.each do |o| %>

    <tr>
		<td>
			<div class="info_created_at">
			    <%= link_to company_path(o.company) do %>
			    	<%= o.company.company_name %>
			    <% end %>
			    	が<%= h.created_at.strftime('%Y/%m/%d') %>にオファー
		    </div>
			<%= link_to offer_path(o) do %>
			  <div class="index-title">
			    <%= h.title %>
			  </div>
			<% end %>
			<div class="info_created_at tag">
		    	<%= o.status %> <%= o.salary %>
		    </div>
		</td>
	</tr>
    <% end %>
</table>

<% else %>

<strong>no offer</strong>

<p>オファーしているAgentが、いません</p>


<% end %>