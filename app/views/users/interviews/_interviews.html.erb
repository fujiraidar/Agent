

<% interviews.each do |i| %>

    <div class="info_created_at">
	    <%= i.created_at.strftime('%Y/%m/%d %H:%M') %><br />
	</div>

	<div class="tag">
		<%= simple_format(i.message) %><br />
	</div><br />
<% end %>